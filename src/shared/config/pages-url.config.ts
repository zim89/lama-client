class PAGES {
  private root = '/'

  HOME = this.root
  LOGIN = `/auth/login`
  REGISTER = `/auth/register`
  CATALOG = `/catalog`
  GIRLS = `/girls`
  BOYS = `/boys`
  BABIES = `/babies`
  MOMS = `/moms`
  DADS = `/dads`
}

export const APP_PAGES = new PAGES()
